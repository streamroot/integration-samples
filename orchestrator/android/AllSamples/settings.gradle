// Sample apps
def samples = [
        'ExoPlayer',
        'ExoPlayer-Java',
        'PRESTOplay-Java',
        'ExoPlayer-Yospace-Java',
        'PRESTOplay-Yospace-Java'
]

samples.each {
    def entry = ':' + it
    include entry
    project(entry).projectDir = new File('../' + it + '/app')
}
